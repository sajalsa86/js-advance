const numbers = [0, 1, 1, 2, 3, 5, 8, 13, 21];
const numbers2 = [34, 55, 89, 144];

const doesExit = numbers.includes(13);
console.log(doesExit);

//add two array
const fibo = numbers.concat(numbers2);
console.log(fibo);